#!/bin/bash

ln -sf "$HOME"/.dotfiles/.bashrc        "$HOME"
ln -sf "$HOME"/.dotfiles/.compton.conf	"$HOME"
ln -sf "$HOME"/.dotfiles/.fonts         "$HOME"
ln -sf "$HOME"/.dotfiles/.vimrc         "$HME"
ln -sf "$HOME"/.dotfiles/.i3            "$HOME"
